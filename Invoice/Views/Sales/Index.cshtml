@model IEnumerable<Invoice.Core.Entity.SalesModel>
@{
    ViewData["Title"] = "Lista Vanzari";
    Layout = "~/Views/Shared/_adminLayout.cshtml";
}



<div class="page-wrapper">
    <div class="container-fluid">
        <!-- Start Page Content -->
        <div class="row">
            <div class="col-12">
                <div class="card">
                    <div class="card-body">
                        <h5 class="card-title">Lista Vanzari</h5>
                        <div class="table-responsive m-t-10">
                            <table id="example23" class="display nowrap table table-hover  table-bordered" cellspacing="0" width="100%">
                                <thead>
                                    <tr>
                                        <th>Serie Factura</th>
                                        <th>Data</th>
                                        <th>Total</th>
                                        <th>Reducere</th>
                                        <th>Total Vanzare</th>
                                        <th>Status Factura</th>
                                        <th>Optiuni</th>
                                    </tr>
                                </thead>
                                <tbody>
                                    @foreach (var item in Model)
                                    {
                                        <tr>
                                            <td>@item.SaleCode</td>
                                            <td>@item.SalesDate.ToString("dd/MM/yyyy")</td>
                                            <td>@item.Total</td>
                                            <td>@item.Discount</td>
                                            <td>@item.GrandTotal</td>
                                            <td>
                                                @if (item.Status == "Due")
                                                {
                                                    <label class="label label-warning">De platit</label>

                                                }
                                                @if (item.Status == "Paid")
                                                {
                                                    <label class="label label-success">Platita</label>

                                                }
                                            </td>
                                            <td>

                                                <a href="@Url.Action("SendEmail","Sales", new {saleId = item.Id})" title="Trimite Email" style="padding:5px;">
                                                    <i class="fa fa-envelope" style="font-size:15px;"></i>
                                                </a>

                                                <a href="@Url.Action("SaleInvoice","Sales", new {saleId = item.Id,style=1})" title="Model Factura 1" style="padding:5px;">
                                                    <i class="fa fa-file" style="font-size:15px;"></i>
                                                </a>

                                                <a href="@Url.Action("SaleInvoice","Sales", new {saleId = item.Id,style=2})" title="Model Factura 2" style="padding:5px;">
                                                    <i class="fa fa-file" style="font-size:15px;"></i>
                                                </a>

                                                <a href="@Url.Action("EditSale", "Sales", new {saleId= item.Id})" title="Editeaza" style="padding:5px;">
                                                    <i class="fa fa-edit" style="font-size:15px;"></i>
                                                </a>
                                                <a href="@Url.Action("DeleteSale", "Sales", new {saleId = item.Id})" title="Sterge" style="padding:5px;" onclick="return confirm('Sigur doresti sa stergi aceasta factura?')">
                                                    <i class="fa fa-trash" style="font-size:15px;"></i>
                                                </a>
                                            </td>
                                        </tr>
                                    }
                                </tbody>
                            </table>
                        </div>
                    </div>
                </div>
            </div>
        </div>
        <!-- End Page Content -->
    </div>
</div>
